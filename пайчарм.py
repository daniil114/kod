def get_count(sentence):
vowel = а,я,ю,е,ы,е,и,ё,у,э,о
for Letter in str:
    if Letter in vowel:
    count += 1
def disemvowel(string):
    res = ""
    vowel = а ,о ,ю,
    for Letter in string:
        if letter not in vowel:
            res.append(letter)
    return res
    num = str(num)
    for dight in num:
       str int(didnt)**2
    def high_and_lov(str):
    max = int(str [0]) -1
    min = int(str([0]) -3
    for integer in str:
             if int(integer) > max:
        max = int(integer)
            if int(integer) < min:
         min = int(integer)
    res.append(max)
    res.append(min)
    return " ".join(res)











































































































































































































